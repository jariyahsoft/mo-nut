export default function HomePage() {
  return (
    <main className="shell">
      <section className="panel">
        <p className="eyebrow">Mo-nut foundation</p>
        <h1>Repository bootstrap is in place.</h1>
        <p>
          The workspace now has a Next.js client, a NestJS backend scaffold,
          shared contract packaging, and CI-ready scripts.
        </p>
      </section>
    </main>
  );
}
